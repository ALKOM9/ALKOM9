# 🔑 קבלת מפתח FRED – 2 דקות מהטלפון

ה-Macro Dashboard (מודול 4) צריך מפתח חינמי ל-FRED API של ה-Federal Reserve של St. Louis. בלעדיו, רוב הקורס עובד – אבל מודול 4 לא יראה נתונים חיים.

## למה צריך את זה

FRED = המאגר הציבורי הגדול בעולם לנתוני מאקרו: עקומת תשואות, אבטלה, אינפלציה, ייצור תעשייתי, ועוד. הוא חינמי ב-100%, ללא הגבלת קריאה משמעותית למשתמש פרטי. רק צריך מפתח אישי שמתקבל מיידית.

## 3 צעדים – הכל מהטלפון

### 1. הירשם

פתח בדפדפן של הטלפון:

**https://fredaccount.stlouisfed.org/apikeys**

לחץ **"Request API Key"**. אם אין לך עדיין חשבון, יבקשו ממך להירשם:
- אימייל
- שם
- סיסמה
- ארגון: כתוב "Personal" או "Self"
- מטרת השימוש: כתוב "Education" או "Personal research"

לאישור האימייל ייקח 0–60 שניות. אם לא מגיע, בדוק ספאם.

### 2. אסוף את המפתח

אחרי שאישרת את האימייל, חזור ל:

**https://fredaccount.stlouisfed.org/apikeys**

תראה מחרוזת של 32 תווים אותיות-קטנות + מספרים. זה המפתח. **העתק אותו** (לחיצה ארוכה → Copy).

⚠️ אל תשתף את המפתח. הוא יושב אצלך ב-Streamlit Secrets, לא בקוד.

### 3. הדבק ב-Streamlit Secrets

פתח את האפליקציה ב-Streamlit Cloud, ואז:

**Settings → Secrets**

הדבק:

```toml
password = "הסיסמה_שלך_לקורס"
fred_api_key = "המפתח_מ_FRED"
```

לחץ **Save**. Streamlit יעשה Reboot אוטומטי. תוך 30 שניות מודול 4 יציג נתונים חיים.

## בדיקה

נכנס לאפליקציה → מודול 4 → אם רואים מספרים עדכניים ליד "Yield Curve (10Y–2Y)" ו-"Unemployment Rate", הכל עובד.

אם רואים "⚠️ לא נמצא fred_api_key": חזור ל-Secrets, ודא שאין רווחים מסביב למפתח ושהשורה היא בדיוק:

```toml
fred_api_key = "abcd1234..."
```

## הגבלות

- FRED מרשה ~120 קריאות לדקה למשתמש. הקורס משתמש ב-cache של 6 שעות, אז אתה רחוק מההגבלה.
- אם מודול 4 איטי בכניסה ראשונה ביום – זה נורמלי, הוא בונה cache. כניסה הבאה תהיה מיידית.
