# 📱 מדריך הקמה מהטלפון – קורס Triangular Arbitrage

המדריך הזה מיועד אך ורק לטלפון (אנדרואיד / iPhone). אין צורך במחשב, אין צורך בטרמינל, אין צורך להתקין Python. הכל קורה דרך הדפדפן של הטלפון.

זמן משוער: **15–25 דקות** בפעם הראשונה. אחרי זה זה "set and forget".

---

## 🎯 התמונה הגדולה לפני שמתחילים

מה הולך לקרות בסך הכל:

1. **GitHub** – שם הקוד של הקורס "חי" (אני, Claude, כותב הכל לשם).
2. **Streamlit Cloud** – שירות חינמי שמריץ את הקוד מ-GitHub כאתר אינטרנט.
3. **הטלפון שלך** – נכנס לאתר דרך כתובת ייעודית, מקליד סיסמה, ולומד.

תרשים זרימה:

```
GitHub (קוד) ───► Streamlit Cloud (מריץ) ───► קישור פרטי בטלפון שלך
                                                  ↑
                                          (סיסמה שרק אתה יודע)
```

חשוב להבין: אתה **לא** צריך להתקין שום דבר על הטלפון. רק להיכנס לאתר.

---

## שלב 1 – פתיחת חשבון GitHub מהטלפון

> אם כבר יש לך חשבון GitHub, דלג לשלב 2.

1. פתח את הדפדפן בטלפון (Chrome באנדרואיד או Safari ב-iPhone).
2. גש ל: **github.com**
3. לחץ על **Sign up**.
4. הזן אימייל → סיסמה חזקה → שם משתמש (זכור אותו, אנחנו נשתמש בו).
5. עבור אימות (CAPTCHA, קוד באימייל).
6. בחר **Free plan**.

**טיפ:** שמור את הסיסמה במנהל הסיסמאות של הטלפון (Google Password Manager / iCloud Keychain). אם תאבד אותה זה כאב ראש.

> אופציונלי: אפשר להתקין את אפליקציית **GitHub** מ-Google Play / App Store. היא לא הכרחית אבל נוחה לצפייה בקוד. כל הפעולות במדריך הזה עובדות גם דרך הדפדפן.

---

## שלב 2 – יצירת Repository פרטי

ה-repository (קיצור: "repo") הוא התיקייה שבה הקוד יושב.

1. בדפדפן, וודא שאתה מחובר ל-GitHub.
2. לחץ על **סימן ה-➕** למעלה משמאל (במובייל לפעמים זה בתוך תפריט המבורגר ☰).
3. בחר **New repository**.
4. **Repository name**: הקלד `triangular-arbitrage-course` (או כל שם אחר, אבל זכור אותו).
5. **Description**: למשל "Personal course – do not share".
6. **חובה**: בחר **Private** (ולא Public). זה מה שמוודא שרק אתה רואה את הקוד.
7. **אל תסמן** "Add a README file" – אנחנו נוסיף את הקבצים ישירות.
8. לחץ **Create repository**.

עכשיו ראית מסך עם הכותרת "Quick setup" וכתובת ארוכה. **העתק את הכתובת הזו** ותן לי אותה (במידה ואני, Claude, צריך לכתוב לשם ישירות; אם הקבצים כבר ב-repo אחר, דלג).

> 📌 **חלופה: אני, Claude, כבר כתבתי את כל הקבצים ל-repo שמנהל אותי.** במצב הזה, פשוט עברו לשלב 3 והשתמש בשם ה-repo שאני נותן לך.

---

## שלב 3 – ודא שכל הקבצים בענף הנכון

הקבצים שלך כבר מועלים ל-branch בשם:

```
claude/triangular-arbitrage-course-8EZO7
```

(branch = ענף, גרסה של הקוד).

מהטלפון:

1. היכנס לעמוד ה-repo ב-GitHub.
2. ראש העמוד מציג תפריט נפתח (כברירת מחדל: `main`). לחץ עליו.
3. בחר את `claude/triangular-arbitrage-course-8EZO7`.
4. וודא שאתה רואה את הקבצים הבאים:
   - `app.py`
   - `requirements.txt`
   - `MOBILE_SETUP_GUIDE.md` (זה הקובץ הזה!)
   - `.streamlit/config.toml`
   - תיקייה `modules/`
   - תיקייה `utils/`

אם אתה רואה אותם – ✅ הכל תקין.

> אופציונלי אבל מומלץ: מזג את ה-branch הזה ל-`main`.
> ב-GitHub, לחץ **Pull requests → New pull request**, ובחר base: `main`, compare: `claude/triangular-arbitrage-course-8EZO7`, ואחר כך **Create pull request → Merge pull request**. ככה Streamlit ירוץ אוטומטית על הגרסה היציבה.
> אם זה מסובך לך – לא נורא, פשוט תגיד ל-Streamlit ב-Cloud להשתמש ב-branch `claude/triangular-arbitrage-course-8EZO7` ישירות (בשלב 4 למטה).

---

## שלב 4 – חיבור Streamlit Community Cloud

Streamlit Cloud הוא שירות **חינמי** של חברת Streamlit שמריץ אפליקציות Python מ-GitHub. נירשם, נחבר ל-GitHub, נפרוס.

### 4.1 הרשמה

1. בטלפון, גש ל: **share.streamlit.io**
2. לחץ **Continue with GitHub**.
3. תועבר ל-GitHub – אשר **Authorize Streamlit**.
4. ייתכן ש-GitHub יבקש סיסמה / 2FA – אשר.

### 4.2 הענקת גישה ל-repo הפרטי

1. אחרי האישור, חזרת לדף Streamlit.
2. לחץ על **Workspaces** → ולוודא שיש לך workspace.
3. לחץ **Create app** (או **New app**).
4. אם זו הפעם הראשונה, ייתכן ש-Streamlit לא רואה את ה-repo הפרטי. במקרה כזה לחץ **"Sign in with GitHub again"** ובמסך ההרשאות סמן **"All repositories"** או באופן ספציפי את ה-repo הזה.

### 4.3 פריסה (Deploy)

במסך **Deploy an app** מלא:

| שדה | מה למלא |
|---|---|
| **Repository** | בחר את ה-repo שלך (למשל `your-username/triangular-arbitrage-course` או השם שאני נותן) |
| **Branch** | `claude/triangular-arbitrage-course-8EZO7` (או `main` אם מיזגת) |
| **Main file path** | `app.py` |
| **App URL** (optional) | בחר כתובת קצרה ויפה. למשל `my-arb-course`. הכתובת תהיה `https://my-arb-course.streamlit.app` |

לחץ **Deploy**.

🕐 חכה 2–5 דקות. תראה לוג מתגלגל. בסוף יופיע: **"Your app is live"**.

---

## שלב 5 – הגדרת הסיסמה (Secrets)

זה החלק שמוודא ש**רק אתה** נכנס. הסיסמה לא נשמרת בקוד אלא בשירות נפרד של Streamlit.

1. במסך האפליקציה שלך ב-Streamlit Cloud, לחץ על **⚙️ Settings** (פינה ימנית עליונה, או שלוש נקודות → Settings).
2. בחר **Secrets** מהתפריט.
3. בחלון העריכה, **הדבק** את השורות הבאות (החלף את `CHOOSE_A_STRONG_PASSWORD` בסיסמה שלך):

```toml
password = "CHOOSE_A_STRONG_PASSWORD"
```

4. לחץ **Save**.
5. חזור למסך האפליקציה ולחץ **Reboot** (או **Restart app**).

### החלפת סיסמה בעתיד

בכל פעם שתרצה לשנות את הסיסמה:

1. שוב: **Settings → Secrets**.
2. עדכן את הערך.
3. **Save** ואז **Reboot**.

זהו. אין צורך לגעת בקוד.

---

## שלב 6 – כניסה ראשונה לאפליקציה

1. פתח את כתובת האפליקציה בטלפון: `https://YOUR-APP.streamlit.app`
2. אם המסך אומר "Yes, get this app back up!" – זה אומר שהאפליקציה נרדמה (חיסכון משאבים). לחץ על הכפתור והמתן ~30 שניות.
3. תראה תיבת **סיסמה** – הקלד את הסיסמה שהגדרת בשלב 5.
4. תראה **מסך הסכמה דו-לשוני** – קרא בעיון, סמן את התיבה, לחץ "אני מסכים / I Agree".
5. ⭐ אתה בקורס.

---

## שלב 7 – הוספה למסך הבית של הטלפון (כמו אפליקציה)

זה לא חובה אבל ממליץ – ככה הקורס מקבל אייקון נפרד וזה מרגיש כמו אפליקציה אמיתית.

### באנדרואיד (Chrome)

1. פתח את כתובת האפליקציה ב-Chrome.
2. לחץ על שלוש הנקודות ⋮ בפינה ימנית למעלה.
3. בחר **"Install app"** או **"Add to Home screen"**.
4. תן שם, למשל "קורס ארביטראז'".
5. **Add**.

### ב-iPhone (Safari)

1. פתח את כתובת האפליקציה ב-Safari (חובה Safari, לא Chrome).
2. לחץ על כפתור השיתוף (ריבוע עם חץ למעלה) בתחתית.
3. גלול ובחר **"Add to Home Screen"** / "הוסף למסך הבית".
4. תן שם.
5. **Add**.

עכשיו יש לך אייקון על מסך הבית, ובלחיצה אחת אתה בקורס.

---

## שלב 8 – שימוש יומיומי

* פתח את האייקון מהמסך → הקלד סיסמה → לחץ הסכמה → התחל לימוד.
* כל מודול: הסבר → מחשבון אינטראקטיבי → חידון.
* התקדמותך **לא נשמרת בין מפגשים** (אין משתמשים, רק אתה) – אבל אם תרצה לראות מודול שוב, פשוט תיגש אליו מהתפריט הצדדי (☰ בפינה).

---

## ❓ תקלות נפוצות

| תקלה | פתרון |
|---|---|
| "Your app has gone to sleep" | לחץ על הכפתור הכחול, חכה 30 שניות. זה נורמלי באפליקציות חינמיות. |
| "ModuleNotFoundError: ..." | ייתכן ש-`requirements.txt` לא מלא. דווח לי – אתקן. |
| "Bad password" | חזור ל-Settings → Secrets בStreamlit Cloud, וודא שאין רווחים מסביב לסיסמה ושיש מירכאות `"..."`. אחרי שינוי – **Reboot**. |
| הטקסט נראה דחוס בטלפון | סובב את הטלפון למצב landscape, או הגדל את גודל הגופן בהגדרות הדפדפן. ה-CSS שלי מותאם למובייל אבל לפעמים זה תלוי דגם. |
| התרשימים חורגים מהמסך | החלק את התרשים שמאלה/ימינה בתוך עצמו. כל תרשים responsive. |
| העמוד ריק / לבן | רענן את הדף. אם זה ממשיך – Reboot את האפליקציה ב-Streamlit Cloud. |
| "GitHub authentication required" | חזור ל-share.streamlit.io והתחבר מחדש עם GitHub. |

---

## 🔒 הערה לגבי פרטיות

* ה-repo שלך **פרטי** ב-GitHub – אף אחד לא רואה את הקוד.
* האפליקציה ב-Streamlit Cloud **פתוחה לאינטרנט** מבחינה טכנית, אבל מוגנת בסיסמה. ככל שהסיסמה חזקה ויחידנית – אף אחד לא ייכנס.
* Streamlit Community Cloud הוא שירות חינמי – אל תשמור שם נתונים אישיים רגישים מעבר לסיסמת הקורס.
* אם תרצה ביטחון נוסף: שנה את הסיסמה כל כמה חודשים.

---

## 🆘 מה לעשות אם משהו לא עובד

1. צלם מסך מהטלפון של המסך / השגיאה.
2. שלח לי (Claude) את הצילום עם תיאור קצר: "ניסיתי X, ראיתי Y".
3. אני אתקן את הקוד ב-GitHub. תוך 1–3 דקות Streamlit Cloud יעדכן את האפליקציה אוטומטית. רענן את העמוד בטלפון – ובדוק.

זהו, נתראה בלימוד. 🚀
